namespace AiAgentEconomy.Domain.Agents
{
    public enum AgentStatus
    {
        Active = 1,
        Paused = 2,
        Suspended = 3
    }

    public enum AgentRiskLevel
    {
        Low = 1,
        Medium = 2,
        High = 3
    }
}
